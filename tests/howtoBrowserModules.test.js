const assert = require('assert');
const howtoBrowserModules = require('../lib/index.js');

describe('howtoBrowserModules', () => {
  it('has a test', () => {
    assert(false, 'howtoBrowserModules should have a test');
  });
});
